# Copy to .env.local (not committed)

# Resend API key (server-side only; do not prefix with NEXT_PUBLIC_)
RESEND_API_KEY=

# Optional: verified From address and recipient
# RESEND_FROM=no-reply@yoload.asia
# RESEND_TO=info@yoload.asia

# Cloudflare Turnstile
# Client-side widget uses NEXT_PUBLIC_TURNSTILE_SITE_KEY
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# Optional alias (not used in code): TURNSTILE_SITE_KEY
TURNSTILE_SITE_KEY=
# Server-side secret for verification
TURNSTILE_SECRET_KEY=

# Optional: simple rate limit (requests per minute per IP; default: 2)
# CONTACT_RATE_LIMIT_PER_MIN=2

# Analytics (switch between plausible|ga4|none)
ANALYTICS_PROVIDER=plausible
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yoload.asia
# If self-hosting Plausible, set your endpoint; otherwise keep https://plausible.io
NEXT_PUBLIC_PLAUSIBLE_API_HOST=https://plausible.io

# Google Analytics 4 (if ANALYTICS_PROVIDER=ga4)
# Public ID used by the client (preferred)
NEXT_PUBLIC_GA_ID=
# Back-compat (optional)
GA_MEASUREMENT_ID=
NEXT_PUBLIC_GA_MEASUREMENT_ID=
